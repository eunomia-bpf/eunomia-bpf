[package]
name = "eserver"
version = "0.1.0"
edition = "2021"

[dependencies]
lib = { path = "../lib"}
tokio = { version = "1.24.2", features = ["rt-multi-thread"] }
clap = { version = "4.0.32", features = ["derive"] }
